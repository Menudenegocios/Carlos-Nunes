
RewriteEngine On

# Impede que o Apache tente servir os arquivos diretamente
# e força tudo a passar pelo Node.js na porta configurada
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://localhost:3000/$1 [P,L]

# Caso o proxy falhe, garante que pelo menos o arquivo index.html seja acessível
DirectoryIndex index.html
